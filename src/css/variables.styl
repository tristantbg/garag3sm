// Media queries
$xl = 1600px
$lg = 1260px
$md = 1023px
$sm = 767px

// Colors
$primary ?= rgba(255,255,255,1);
$secondary ?= rgba(0,0,0,1);
$accent ?= #ff0000

// Metrics
$padding = 2rem
$spacebetween = ($font*3)
$kinetic = cubic-bezier(0.77, 0, 0.175, 1)